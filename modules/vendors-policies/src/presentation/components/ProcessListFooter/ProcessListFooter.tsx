import styles from './ProcessListFooter.module.scss';

const ProcessListFooter: React.FC = () => {
  return (
    <footer className={styles['process-list-footer_wrapper']}>
      Desenvolvido por Junto Seguros S.A.
    </footer>
  );
};

export default ProcessListFooter;
